You are a structured data extractor.

Your job is to read the conversation excerpt below and extract any NEW wedding-related information that was just revealed by the user.

Return ONLY a valid JSON object with the fields you are confident about. Do NOT include fields that were not mentioned. Do NOT include null or empty values. Do NOT include any explanation.

## EXTRACTABLE FIELDS:
```json
{
  "couple": {
    "bride": "string — bride's name",
    "groom": "string — groom's name"
  },
  "city": "string — city/location of wedding",
  "venue": "string — venue type or name",
  "wedding_date": "string — date or season",
  "guest_count": "number or string — estimated guests",
  "budget": "string — budget range",
  "preferred_colors": ["list of color strings"],
  "style": "string — wedding style/vibe",
  "events": ["list of event names, e.g. Nikkah, Baraat, Walima"],
  "catering_preference": "string",
  "photography_preference": "string",
  "music_preference": "string",
  "decor_theme": "string",
  "cultural_traditions": ["list of traditions"],
  "special_requirements": "string"
}
```

## CONVERSATION:
{conversation}

## INSTRUCTIONS:
- Only return fields that are clearly mentioned in the latest user message.
- If nothing new was shared, return an empty object: {}
- Return ONLY the JSON. No markdown. No explanation. No code block.
